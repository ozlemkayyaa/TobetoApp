import 'package:flutter/material.dart';
import 'package:tobeto/app.dart';

void main() {
  // Add Widgets Binding
  // Inıt Local Storage
  // Await Native Splash
  // Initialize Firebase
  // Initialize Authentication

  runApp(const MyApp());
}
